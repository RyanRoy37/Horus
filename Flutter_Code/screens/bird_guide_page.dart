import 'package:flutter/material.dart';

class BirdGuidePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Bird Guide')),
      body: Center(child: Text('List of birds goes here')),
    );
  }
}
