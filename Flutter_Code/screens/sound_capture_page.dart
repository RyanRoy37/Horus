import 'package:flutter/material.dart';

class SoundCapturePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Capture Sound')),
      body: Center(child: Text('Microphone functionality coming soon')),
    );
  }
}
